name: Notify website of resume update

on:
  push:
    branches: [main]
    paths: [resume.pdf]

jobs:
  trigger-vercel-redeploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy hook
        run: curl -fsS -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_URL }}"
